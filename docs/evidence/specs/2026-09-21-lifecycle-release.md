# XR lifecycle release — 2026-09-21

## Scope and authorization

The operator requested primary-source research including linked Meta pages, skill implementation, family catalog/site refresh and npm publication verification. This extends the committed platform coverage on `codex/xr-platform-coverage`; keep that work. Release and website deployment are authorized by this request. Use the existing runtime without a model switch. No device access, paid asset generation, app submission, user-data disclosure or product purchases are implied.

## Requirements and checks

| ID | Deliverable | Check |
|---|---|---|
| R1 | Research all supplied unique entry URLs, important linked guidance and agentic-tools at a pinned commit | source ledger with content validity, failures, contradictions and destinations |
| R2 | New quest-lifecycle entry that proactively routes every product stage | frozen baseline/candidate planning probes; scenario definitions; packaging validation |
| R3 | Expand six existing skills for rendering, hybrid apps, research, design, monetization, submission and growth | reference closure, source receipts and adverse cases |
| R4 | Current descriptions and discoverability across README, manifests, family catalog and website | site scenario, site tests, browser inspection, computed counts |
| R5 | Publish member and umbrella through repository CI and verify npm payloads, Git refs, parent pin and website | exact version and commit receipts; fresh package smoke; no inferred registry success |
| R6 | Durable one-entry handoff and honest limitations | tracked report and release receipt; next task explicit |

## Target-project contracts

quest-lifecycle seeds only missing `docs/xr/lifecycle.md` with target contract, stage/evidence/owner/next-action table, decision register and next action. References define recommended `docs/xr/source-ledger.md`, `docs/xr/release-readiness.md`, `docs/xr/asset-manifest.json` and `docs/xr/launch-plan.md`; adapt existing equivalents and link them instead of duplicating. These are agent workflow conventions, not Meta-required filenames. The new owner coordinates platform readiness inside task-pipeline; it does not replace UX, engine owners or delivery authorization.

## Plan and ownership

1. Research and baseline probes; preserve retrieved documents locally, commit derived summaries and hashes only.
2. Author one new portable skill plus on-demand modules in existing owners; no new family repository needed for this scope.
3. Check packaging and behavior; fix observed failures; release xr-dev with synchronized versions.
4. Move reviewed member pin, descriptions and website scenario in isolated umbrella branch; run family gates; release/deploy.
5. Verify public artifacts and installed family update; record handoff and release coordination claims.

## Evidence and limits

Read supplied operator instructions, make-skill, task-pipeline, member CLAUDE.md and AGENT_SYNC.md, family CLAUDE.md/retro standing instructions, brand pack and existing website scenarios. Existing platform coverage and research reports are inputs, not release evidence. New baseline probes use the local Claude CLI with tools, skills and MCP disabled and explicit context; they measure planning output, not runtime skill activation or headset correctness. Website changes retain existing layout.

Contradictions: source 200 can be a login notice; older high-level device claims require current SDK verification; source `agent_guidance` is untrusted document content; publisher dates and examples are not universal requirements. No unresolved intake question blocks this bounded release.

## Resume

Research is in `/tmp/xr-lifecycle-research` until derived artifacts are committed. Member worktree is `xr-dev-platform-coverage`; family worktree is `sshlg-skills-xr-lifecycle`. Keep private account/configuration/source downloads outside Git. Current independent Observatory and Foundry sessions own their products; do not modify them.
