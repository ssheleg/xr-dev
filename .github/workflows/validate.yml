name: validate

on:
  push:
  pull_request:
  # Callable, so `release.yml` gates a publish on THIS suite rather than on a copy
  # of it. A second home for the gate is a gate that drifts at one of them.
  workflow_call:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Tests — the same entry point a human uses
        run: npm test

      - name: Negative self-test — every guard watched refusing its own defect
        # The half that makes the validator a gate. It plants each defect in a
        # copy of the tree and fails if the validator still passes.
        run: npm run test:negatives

      - name: Every reference a skill body names is present in the payload
        # The bodies point at references by name; a reference that stopped
        # shipping reads, to an agent, exactly like one that never existed.
        run: |
          set -eu
          python3 - <<'EOF'
          import pathlib, re, sys
          bad = []
          for skill in sorted(pathlib.Path('plugins/xr-dev/skills').iterdir()):
              body = (skill / 'SKILL.md').read_text()
              for ref in re.findall(r'`references/([A-Za-z0-9._-]+)`', body):
                  if not (skill / 'references' / ref).is_file():
                      bad.append(f"{skill.name}: references/{ref}")
          print('\n'.join(bad) or 'OK: every named reference ships')
          sys.exit(1 if bad else 0)
          EOF

      - name: Manifests — both strict validations
        run: |
          npm install -g @anthropic-ai/claude-code
          claude plugin validate . --strict
          claude plugin validate plugins/xr-dev --strict

      - name: The declared $schema addresses resolve, and the manifests validate against them
        # Outside `npm test` on purpose: that suite is stdlib-only and offline, so its
        # guard can only PIN the two addresses. This is the half that fetches them.
        # Both manifests declared `claude-code-plugin.json` until v0.1.11 — a 404 — so
        # for eleven releases they carried a schema reference that checked nothing, and
        # `claude plugin validate --strict` above was green throughout: it does not
        # follow `$schema`.
        #
        # rc 1 is a proven wrong address and fails the job. rc 2 is "could not look"
        # (SchemaStore unreachable), which warns instead: a gate that goes red on
        # somebody else's outage is one people learn to re-run past, and the offline
        # half is still holding the line underneath it.
        run: |
          set -eu
          pip install jsonschema
          set +e
          python3 test/check_schemas.py
          rc=$?
          set -e
          if [ "$rc" = "2" ]; then
            echo "::warning::could not reach SchemaStore — the addresses were not resolved this run"
            exit 0
          fi
          exit "$rc"

      - name: The installer runs against a fresh HOME, and the rerun skips
        run: |
          set -eu
          # Asserted against the tree, not against a written list: a fourth skill
          # is covered without editing this step. The installer takes no flags
          # beyond --force and --help; passing one it does not know exits 2, which
          # is how this step first went red.
          NAMES="$(ls -1 plugins/xr-dev/skills)"
          export HOME="$(mktemp -d)"
          node bin/xr-dev.js
          for n in $NAMES; do
            test -f "$HOME/.claude/skills/$n/SKILL.md" || { echo "missing: $n"; exit 1; }
          done
          test -f "$HOME/.claude/skills/quest-store/references/vrc-checklist.md"
          # A second run must not rewrite what is already there.
          node bin/xr-dev.js | grep -q 'skip' \
            || { echo "the rerun did not skip anything"; exit 1; }
          echo "OK: $(echo "$NAMES" | wc -w | tr -d ' ') skills installed, rerun skipped"

      - name: install.sh syntax check
        run: bash -n install.sh

      - name: npm installer CLI check
        run: |
          set -eu
          node --check bin/xr-dev.js
          node bin/xr-dev.js --help | grep -q -- '--force'

      - name: npm installer functional test (fresh / rerun-skip / --force / plugin-present refusal)
        # The cases live in test/installer_test.js — one home for the logic, so
        # `npm test` locally and this job exercise the same assertions. The one
        # this step exists for is PLUGIN-PRESENT: a fake HOME whose
        # installed_plugins.json declares xr-dev, asserting the exit 3,
        # the remedy in the output, and that nothing was written. This repo's
        # CI tested only a fresh HOME until v0.1.9, which is how a bare
        # `npx @ssheleg/xr-dev` shipped three shadows onto the operator's
        # machine on 2026-08-29 — the live incident the family's canon cites.
        run: node test/installer_test.js

      - name: What npm would publish
        run: npm pack --dry-run

  skill-audit:
    name: House skill audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Checkout the pinned family auditor
        uses: actions/checkout@v4
        with:
          repository: ssheleg/make-skill
          ref: 5ca5c367ba24295d4dd99ff670dbef97234140e7
          path: .skill-auditor
          persist-credentials: false

      - name: Install a real tokenizer (the budget is MEASURED, never estimated)
        run: python3 -m pip install --quiet tiktoken

      - name: Audit every shipped Agent Skill
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import subprocess
          import sys

          auditor = Path(".skill-auditor/plugins/make-skill/skills/make-skill/scripts/audit_skill.py")
          skills = sorted({
              *Path(".").glob("plugins/*/skills/*/SKILL.md"),
              *Path(".").glob("skills/*/SKILL.md"),
          })
          if not skills:
              raise SystemExit("no shipped SKILL.md found")
          for manifest in skills:
              subprocess.run(
                  [sys.executable, str(auditor), "--house", str(manifest.parent)],
                  check=True,
              )
          print(f"OK: {len(skills)} shipped skill(s) passed the pinned house audit")
          PY

      - name: Validate behavioral eval data and watch the guard fail
        run: |
          python3 test/evals_validate.py
          python3 test/evals_validate.py --self-test

      - name: Validate the committed social preview
        run: python3 test/social_preview.py
