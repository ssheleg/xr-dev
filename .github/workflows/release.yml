name: release

# Project-configurable, individually toggleable release automation.
#
# OFF by default: `release` runs only when the repo variable RELEASE_ENABLED is
# "true", `publish` only when PUBLISH_NPMJS is "true" (Settings -> Secrets and
# variables -> Actions -> Variables). A fork decides for itself whether to arm
# either half — no code change needed.
#
# Trigger: a v* tag, or a manual run naming an existing tag. Steps: validate ->
# version check -> GitHub release carrying the CHANGELOG section -> npx smoke
# test from a clean cwd -> npm publish with provenance.

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing vX.Y.Z tag to release and publish (defaults to the ref this runs on)"
        required: false
        type: string

jobs:
  # The whole validate suite, including every negative self-test, before anything
  # is published. Until 2026-08-14 a red `validate` did not stop a release: this
  # repository tagged v0.4.1 while its own validate run failed, and npm served it.
  validate:
    uses: ./.github/workflows/validate.yml

  release:
    needs: validate
    if: vars.RELEASE_ENABLED == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Resolve the tag to release
        id: t
        run: |
          set -eu
          TAG="${{ inputs.tag || github.ref_name }}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "ver=${TAG#v}" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.t.outputs.tag }}
          fetch-depth: 0

      - name: The tag must be reachable from the default branch
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -eu
          # A TAG PUSHED WITHOUT ITS BRANCH is a published release a fresh clone
          # cannot reach, and nothing fails loudly: this workflow fires on the
          # tag, so the release is created, npm publishes, and `origin/main`
          # still reads the previous era. Hit twice here — the `v1.4.0` ghost on
          # 2026-08-04 and `v1.18.0` on 2026-08-12, where `main` was two commits
          # ahead of `origin/main` while the tag was public and served by npm.
          #
          # Checked against the repository's ACTUAL default branch, not a
          # hardcoded `main`: a fork that renamed it would otherwise be told its
          # correct tag is unreachable.
          DEF="$(gh api "repos/$GITHUB_REPOSITORY" --jq .default_branch)"
          git fetch --no-tags origin "$DEF"
          if ! git merge-base --is-ancestor HEAD "origin/$DEF"; then
            echo "::error::${{ steps.t.outputs.tag }} points at a commit that is not an" \
                 "ancestor of origin/$DEF. The tag was pushed and the branch was not, so a" \
                 "clone cannot reach this release. Push the branch, then re-run this job."
            exit 1
          fi
          echo "$(git rev-parse --short HEAD) is an ancestor of origin/$DEF"

      - name: Structural validator (gate the release on a valid repo)
        run: |
          set -eu
          python3 test/validate.py

      - name: Tag must match the manifests (version sync)
        run: |
          set -eu
          PV=$(python3 -c "import json;print(json.load(open('plugins/xr-dev/.claude-plugin/plugin.json'))['version'])")
          if [ "$PV" != "${{ steps.t.outputs.ver }}" ]; then
            echo "tag ${{ steps.t.outputs.tag }} does not match $PV"; exit 1
          fi

      - name: Extract this version's CHANGELOG section
        run: |
          set -eu
          # Accept every heading shape this repo has used: `## v0.2.0`,
          # `## [0.2.0] — date`, `## 0.2.0`. A pattern matching only `## v`
          # shipped broken with v0.1.0 — which was never tagged, so nothing ran
          # it — and failed the first time a release actually used it. The house
          # validator is deliberately permissive here (`^## \[?v?X.Y.Z\]?`);
          # this must be too, or the two gates disagree about what a heading is.
          awk -v v="${{ steps.t.outputs.ver }}" '
            $0 ~ "^## \\[?v?" v "\\]?([^0-9]|$)" {f=1; next}
            /^## \[?v?[0-9]+\.[0-9]+\.[0-9]+/ {f=0}
            f {print}
          ' CHANGELOG.md > /tmp/notes.md
          test -s /tmp/notes.md || { echo "no CHANGELOG section for ${{ steps.t.outputs.ver }}"; exit 1; }
          echo "release notes start:"; head -3 /tmp/notes.md

      - name: Create or refresh the GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -eu
          # Re-runnable: creating a release that exists is an error, and that used
          # to abort the run before anything downstream could happen.
          if gh release view "${{ steps.t.outputs.tag }}" >/dev/null 2>&1; then
            gh release edit "${{ steps.t.outputs.tag }}" --notes-file /tmp/notes.md
            echo "release ${{ steps.t.outputs.tag }} already existed — notes refreshed"
          else
            gh release create "${{ steps.t.outputs.tag }}" \
              --title "${{ steps.t.outputs.tag }} — xr-dev" \
              --notes-file /tmp/notes.md --latest
          fi

      - name: Post-release smoke test (npx from a clean cwd — the verify gate)
        run: |
          set -eu
          # Derived from the repo, never hardcoded: a fourth skill must not need
          # an edit here to be covered. The list this asserts against is the list
          # the installer reads from.
          SRC="$PWD/plugins/xr-dev/skills"
          NAMES="$(ls -1 "$SRC")"
          test -n "$NAMES"
          export HOME="$(mktemp -d)"
          cd "$(mktemp -d)"
          npx --yes "github:ssheleg/xr-dev#${{ steps.t.outputs.tag }}"
          for n in $NAMES; do
            test -f "$HOME/.claude/skills/$n/SKILL.md" \
              || { echo "missing skill after install: $n"; exit 1; }
          done
          # A skill's references and fixtures travel with it, or the skill arrives
          # as a body full of links to nothing. One of each, named rather than
          # counted, because a missing directory is the failure being tested.
          test -f "$HOME/.claude/skills/quest-native/references/manifest-and-gradle.md"
          test -f "$HOME/.claude/skills/quest-perf/references/capture-playbook.md"
          test -f "$HOME/.claude/skills/quest-store/references/vrc-checklist.md"
          # And the shipped fixture still runs from the installed copy.
          echo "OK: released ${{ steps.t.outputs.tag }} installs $(echo "$NAMES" | wc -w | tr -d ' ') skills cleanly via npx"

  publish:
    # `npm publish` used to be the one human step in every release: 2FA blocks a
    # plain token, so it was run by hand and the registry drifted behind the tags
    # — six of seven packages were behind when this was written. Armed per
    # repository by the PUBLISH_NPMJS variable.
    #
    # Auth works two ways on purpose. NODE_AUTH_TOKEN comes from the NPM_TOKEN
    # secret (it must be a GRANULAR AUTOMATION token — a classic token is still
    # refused by 2FA). `id-token: write` is granted regardless, which is what npm
    # trusted publishing (OIDC) needs: name this workflow as the package's trusted
    # publisher on npmjs.com, delete the secret, and this file keeps working with
    # no long-lived credential at all. It also signs provenance.
    needs: release
    if: vars.PUBLISH_NPMJS == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Resolve the tag to publish
        id: t
        run: |
          set -eu
          TAG="${{ inputs.tag || github.ref_name }}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "ver=${TAG#v}" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v4
        with:
          ref: ${{ steps.t.outputs.tag }}

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: npm must be new enough for trusted publishing
        run: |
          set -eu
          # OIDC publishing needs npm >= 11.5.1 and the bundled npm is not
          # guaranteed to be; without this the failure is an opaque auth error.
          npm install -g npm@latest
          npm --version

      - name: The tag must match what would be published
        run: |
          set -eu
          PV=$(node -p "require('./package.json').version")
          if [ "$PV" != "${{ steps.t.outputs.ver }}" ]; then
            echo "tag ${{ steps.t.outputs.tag }} != package.json $PV"; exit 1
          fi

      - name: Is this version already on the registry?
        id: pub
        run: |
          set -eu
          NAME=$(node -p "require('./package.json').name")
          echo "name=$NAME" >> "$GITHUB_OUTPUT"
          # Publishing over an existing version is a hard 403, so a re-run or a
          # tag pushed after a manual publish must not fail the release.
          if npm view "$NAME@${{ steps.t.outputs.ver }}" version >/dev/null 2>&1; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            echo "$NAME@${{ steps.t.outputs.ver }} already published — nothing to do"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: npm publish
        if: steps.pub.outputs.skip == 'false'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --provenance

      - name: The registry must actually serve it
        if: steps.pub.outputs.skip == 'false'
        run: |
          set -eu
          # The read replica lags the write master, and npm says so on every publish:
          # "Your package is being processed and may take a few minutes to become
          # available." "Published" is a claim until it is served, so this polls
          # rather than trusting publish's exit code.
          #
          # This step is the SAME in all ten repositories on purpose, because the two
          # things it gets right were learned separately and each cost a red release:
          #
          # - **Ten minutes, not three.** Measured in xr-dev on 2026-08-25, its
          #   first publish: `npm publish` printed `+ …@0.1.3` and signed provenance,
          #   and the replica served nothing for over three minutes. A first publish
          #   creates the package document, which propagates more slowly than a new
          #   version of an existing one. The fix lived in that one member for six
          #   weeks; on 2026-09-10 sshlg-skills@1.48.1 hit the identical wall in a
          #   workflow that had never received it, and the registry served the version
          #   minutes after the job gave up.
          # - **Ask for the VERSION, not the tag.** `npm view <name> version` answers
          #   "what is the latest dist-tag", which is a different question: it differs
          #   while the tag lags, and it differs permanently for a patch published
          #   behind a newer minor. That one was found in sshlg-skills and xr-dev
          #   had never hit it.
          #
          # A lesson fixed in one member and left in the other nine is not fixed.
          NAME="${{ steps.pub.outputs.name }}"
          WANT="${{ steps.t.outputs.ver }}"
          for i in $(seq 1 60); do
            got=$(npm view "$NAME@$WANT" version 2>/dev/null || echo "")
            if [ "$got" = "$WANT" ]; then
              echo "OK: registry serves $NAME@$got (after ${i}0s)"
              exit 0
            fi
            sleep 10
          done
          # The two failures need different answers, so name which one this is.
          if npm view "$NAME" name >/dev/null 2>&1; then
            echo "::error::the registry knows $NAME but does not serve $WANT after 10 minutes."
            echo "::error::If the publish step printed '+ $NAME@$WANT' the write landed and the"
            echo "::error::read lags: re-run this job — it reports 'already on the registry' and"
            echo "::error::skips rather than publishing twice."
          else
            echo "::error::the registry does not know $NAME at all after 10 minutes — if this"
            echo "::error::was a first publish, check the publish step: it prints '+ $NAME@$WANT'"
            echo "::error::on success, and this step is a propagation check, not a publish check"
          fi
          exit 1
